user nginx;
worker_process 2;

events {
    worker_connections 768;
}

http {
    server_tokens off;
    charset utf-8;
    include /etc/nginx/mime.types;

    access_log /var/log/nginx-access.log;
    error_log /var/log/nginx-error.log;

    server {
        server_name localhost;
        listen <port>;

        location /feature/ {
            proxy_pass http://<host>:<port>/feature/;
        }
    }
}
